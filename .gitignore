# Personal TV Time export data — contains tokens, IPs, personal info. Never commit.
tvtime/

node_modules/
dist/
*.local
.DS_Store

# The local sync token, and wrangler's local D1 + build state.
.dev.vars
.wrangler/
